{
  "entities": {
    "Recipe": {
      "title": "Recipe",
      "description": "Represents a cooking recipe with ingredients, steps, and metadata.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "heroImageUrl": { "type": "string" },
        "videoUrl": { "type": "string" },
        "servings": { "type": "number" },
        "prepTime": { "type": "number", "description": "Prep time in minutes" },
        "labels": { "type": "array", "items": { "type": "string" } },
        "ingredients": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "amount": { "type": "number" },
              "unit": { "type": "string" }
            }
          }
        },
        "steps": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "text": { "type": "string" },
              "imageUrl": { "type": "string" }
            }
          }
        },
        "ownerId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["title", "ownerId", "ingredients", "steps"]
    },
    "Label": {
      "title": "Label",
      "description": "A user-defined label for categorizing recipes.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "ownerId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "ownerId"]
    },
    "UserSettings": {
      "title": "User Settings",
      "description": "User-specific settings and preferences.",
      "type": "object",
      "properties": {}
    }
  },
  "firestore": {
    "/recipes/{recipeId}": {
      "schema": "Recipe",
      "description": "Collection of user recipes."
    },
    "/labels/{labelId}": {
      "schema": "Label",
      "description": "Collection of user-defined labels."
    },
    "/user_settings/{userId}": {
      "schema": "UserSettings",
      "description": "User-specific settings and preferences."
    }
  }
}
